/**
 * @fileoverview Middleware for Fastify Kick-Start library
 * 
 * This module exports common middleware functions that can be used
 * with the @Middleware decorator or registered globally.
 */

// Authentication middleware
export * from './auth.middleware';

// Common middleware
export * from './common.middleware';
